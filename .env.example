BOT_TOKEN=PASTE_TOKEN_HERE
ADMIN_TG_ID=0000000000

DB_PATH=/opt/stock_bot/data/stock.db
EXPORT_DIR=/opt/stock_bot/exports
BACKUP_DIR=/opt/stock_bot/backups

CURRENCY=USD
DECIMALS=2

# Admin / Settings — Site Lock
# Set this once to bootstrap the shared password on first startup.
# After that, change the password from Admin → Settings in the UI.
SITE_LOCK_PASSWORD=
